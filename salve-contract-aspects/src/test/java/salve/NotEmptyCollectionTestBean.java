package salve;

import java.util.Collection;
import java.util.List;
import java.util.Set;

import salve.contract.NotEmpty;

/**
 * 
 * @author igor.vaynberg
 */
class NotEmptyCollectionTestBean
{

    public void testSet(@NotEmpty Set< ? > a)
    {
    }

    public void testList(@NotEmpty List< ? > a)
    {
    }

    public void test0(@NotEmpty Collection< ? > a)
    {
    }

    public void test1(@NotEmpty Collection< ? > a, @NotEmpty Collection< ? > b)
    {
    }

    public void test2(@NotEmpty Collection< ? > a, @NotEmpty Collection< ? > b,
            @NotEmpty Collection< ? > c)
    {
    }

    public void test3(@NotEmpty Collection< ? > a, @NotEmpty Collection< ? > b,
            @NotEmpty Collection< ? > c, @NotEmpty Collection< ? > d)
    {
    }

    public void test4(@NotEmpty Collection< ? > a, @NotEmpty Collection< ? > b,
            @NotEmpty Collection< ? > c, @NotEmpty Collection< ? > d, @NotEmpty Collection< ? > e)
    {
    }

    public void test5(@NotEmpty Collection< ? > a, @NotEmpty Collection< ? > b,
            @NotEmpty Collection< ? > c, @NotEmpty Collection< ? > d, @NotEmpty Collection< ? > e,
            @NotEmpty Collection< ? > f)
    {
    }

    public void test6(@NotEmpty Collection< ? > a, @NotEmpty Collection< ? > b,
            @NotEmpty Collection< ? > c, @NotEmpty Collection< ? > d, @NotEmpty Collection< ? > e,
            @NotEmpty Collection< ? > f, @NotEmpty Collection< ? > g)
    {
    }

    public void test7(@NotEmpty Collection< ? > a, @NotEmpty Collection< ? > b,
            @NotEmpty Collection< ? > c, @NotEmpty Collection< ? > d, @NotEmpty Collection< ? > e,
            @NotEmpty Collection< ? > f, @NotEmpty Collection< ? > g, @NotEmpty Collection< ? > h)
    {
    }

    public void test8(@NotEmpty Collection< ? > a, @NotEmpty Collection< ? > b,
            @NotEmpty Collection< ? > c, @NotEmpty Collection< ? > d, @NotEmpty Collection< ? > e,
            @NotEmpty Collection< ? > f, @NotEmpty Collection< ? > g, @NotEmpty Collection< ? > h,
            @NotEmpty Collection< ? > i)
    {
    }

    public void test9(@NotEmpty Collection< ? > a, @NotEmpty Collection< ? > b,
            @NotEmpty Collection< ? > c, @NotEmpty Collection< ? > d, @NotEmpty Collection< ? > e,
            @NotEmpty Collection< ? > f, @NotEmpty Collection< ? > g, @NotEmpty Collection< ? > h,
            @NotEmpty Collection< ? > i, @NotEmpty Collection< ? > j)
    {
    }


    public NotEmptyCollectionTestBean()
    {
    }


    public NotEmptyCollectionTestBean(@NotEmpty Collection< ? > a)
    {
    }

    public NotEmptyCollectionTestBean(@NotEmpty Collection< ? > a, @NotEmpty Collection< ? > b)
    {
    }

    public NotEmptyCollectionTestBean(@NotEmpty Collection< ? > a, @NotEmpty Collection< ? > b,
            @NotEmpty Collection< ? > c)
    {
    }

    public NotEmptyCollectionTestBean(@NotEmpty Collection< ? > a, @NotEmpty Collection< ? > b,
            @NotEmpty Collection< ? > c, @NotEmpty Collection< ? > d)
    {
    }

    public NotEmptyCollectionTestBean(@NotEmpty Collection< ? > a, @NotEmpty Collection< ? > b,
            @NotEmpty Collection< ? > c, @NotEmpty Collection< ? > d, @NotEmpty Collection< ? > e)
    {
    }

    public NotEmptyCollectionTestBean(@NotEmpty Collection< ? > a, @NotEmpty Collection< ? > b,
            @NotEmpty Collection< ? > c, @NotEmpty Collection< ? > d, @NotEmpty Collection< ? > e,
            @NotEmpty Collection< ? > f)
    {
    }

    public NotEmptyCollectionTestBean(@NotEmpty Collection< ? > a, @NotEmpty Collection< ? > b,
            @NotEmpty Collection< ? > c, @NotEmpty Collection< ? > d, @NotEmpty Collection< ? > e,
            @NotEmpty Collection< ? > f, @NotEmpty Collection< ? > g)
    {
    }

    public NotEmptyCollectionTestBean(@NotEmpty Collection< ? > a, @NotEmpty Collection< ? > b,
            @NotEmpty Collection< ? > c, @NotEmpty Collection< ? > d, @NotEmpty Collection< ? > e,
            @NotEmpty Collection< ? > f, @NotEmpty Collection< ? > g, @NotEmpty Collection< ? > h)
    {
    }

    public NotEmptyCollectionTestBean(@NotEmpty Collection< ? > a, @NotEmpty Collection< ? > b,
            @NotEmpty Collection< ? > c, @NotEmpty Collection< ? > d, @NotEmpty Collection< ? > e,
            @NotEmpty Collection< ? > f, @NotEmpty Collection< ? > g, @NotEmpty Collection< ? > h,
            @NotEmpty Collection< ? > i)
    {
    }

    public NotEmptyCollectionTestBean(@NotEmpty Collection< ? > a, @NotEmpty Collection< ? > b,
            @NotEmpty Collection< ? > c, @NotEmpty Collection< ? > d, @NotEmpty Collection< ? > e,
            @NotEmpty Collection< ? > f, @NotEmpty Collection< ? > g, @NotEmpty Collection< ? > h,
            @NotEmpty Collection< ? > i, @NotEmpty Collection< ? > j)
    {
    }
}
